module.exports.build = function (userLang, callback, token) {
    const lang = userLang && userLang.toLowerCase ? userLang.toLowerCase() : 'fr';
    const resources = require(`./${lang}.json`);

    return function (key) {
        return resources[key];
    };
};
