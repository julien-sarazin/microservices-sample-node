module.exports = app => {
    const Concrete = app.models.Concrete;

    return Action({
        execute: (context) => {
            let concrete = new Concrete(context.data);
            concrete.owner = context.user.id;

            return concrete.save()
                .then(context.created)
        }
    });
};