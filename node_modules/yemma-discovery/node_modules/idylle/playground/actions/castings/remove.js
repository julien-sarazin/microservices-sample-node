module.exports = app => {
    const Casting = app.models.Casting;

    return Action({
        execute: (context) => {
            return Casting
                .findByIdAndRemove(context.params.id)
                .then(Utils.ensureOne({code: 404, reason: 'casting.not.found'}))
                .then(context.noContent);
        }
    });
};
